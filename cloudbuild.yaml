steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/skinfitcup:latest', '.']
  
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/skinfitcup:latest']
  
  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'skinfitcup'
      - '--image'
      - 'gcr.io/$PROJECT_ID/skinfitcup:latest'
      - '--region'
      - 'europe-west1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--max-instances'
      - '10'
      - '--set-env-vars'
      - 'NODE_ENV=production,SESSION_SECRET=${_SESSION_SECRET},CORS_ORIGINS=${_CORS_ORIGINS}'
      - '--port'
      - '8080'

substitutions:
  _SESSION_SECRET: 'mvPbJoYgut/V0nMbE6YAAuBE9DqCDzx5Bc5H4nMYrD0='
  _CORS_ORIGINS: 'https://sfc-rsv.de,https://www.sfc-rsv.de,https://skinfitcup-*.run.app'

images:
  - 'gcr.io/$PROJECT_ID/skinfitcup:latest'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY
